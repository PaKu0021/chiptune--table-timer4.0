Chiptune 桌位管理系统 v4.0.59

iPad 同步锁修复：

1. iPad不再等待可能卡死的Safari跨标签页Web Lock。
2. 改用当前页面内单任务上传，同一页面不会重复并发同步。
3. 多页面偶发重放仍由Firestore事务和operationId去重，不会重复生成账单或付款。
4. 上传函数进入前发生的错误也会显示在同步详情中。
5. Mac和其他桌面浏览器继续使用跨标签页同步锁。

版本：v4.0.59
