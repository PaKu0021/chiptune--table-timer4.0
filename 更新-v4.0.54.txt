Chiptune 桌位管理系统 v4.0.54

同步性能优化：

1. 同一账单的账单资料、付款明细和作废操作合并为一次 Firestore 事务。
2. 上传数量改为按“合并批次”计算，不再把同一账单的每笔付款分别显示为一项。
3. 不同账单继续并发上传；iPad 最多同时处理6个账单批次，避免连接过多。
4. 每个原始 operationId、付款稳定ID和并发冲突检查全部保留，不以删除历史操作换取速度。
5. 队列只在合并事务成功后才删除，断网或页面退到后台仍可安全重试。

版本：v4.0.54
