Chiptune v4.0.51

1. 首页改为严格只读，不再在账单列表尚未加载完成时自动生成或覆盖进行中账单。
2. 单桌开始必须由 Firestore 事务确认桌位可用；离线、超时或服务器失败时恢复原状态，不生成本机幽灵计时和账单。
3. 预约组编号必须由云端事务唯一分配，取消可能与其他设备重复的本机顺序编号回退。
4. 云端组编号超过 3 秒仍未取得时明确提示重试；批量开始不会静默失败。
5. 全站静态资源缓存版本更新为 v4.0.51，避免 iPad 继续使用旧模块。

说明：
Firestore 当前尚未接入员工登录。本版本不直接收紧安全规则，以免现有店内设备全部失去访问权限。
