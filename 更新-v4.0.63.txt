Chiptune v4.0.63

- 修复 IndexedDB 打开请求偶发永久等待，导致同步函数停在读取队列阶段、一直显示待上传且没有失败信息的问题。
- IndexedDB 连接改为页面内复用，避免每次读写都新建连接并逐渐累积阻塞。
- 打开本地数据库超过 2.5 秒会立即切换到已有的应急队列继续上传，不再卡住云端同步。
- 连接迟到时会主动关闭，避免旧页面继续占用数据库。
- v4.0.62 的浏览器 Web Lock 修复和 Firestore 幂等保护继续保留。
