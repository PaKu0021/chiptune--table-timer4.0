Chiptune v4.0.66

- 修复 Safari/IndexedDB 删除提交延迟时，已完成的待办又被重新合并回本机队列的问题。
- 为桌位队列和账单队列加入 operationId 删除确认标记（tombstone）。
- 云端事务完成后立即从可见队列排除已确认项目，同时继续尝试清理 IndexedDB 原记录。
- 新的同 ID 项重新入队时会自动撤销旧删除标记，不影响正常重试。

