Chiptune 桌位管理系统 v4.0.52

修复内容：

1. 修复 iPad 开始计时时出现“服务器锁定桌位超时”的问题。
2. 单桌开始的权威事务现在只锁定当前桌位，并同时写入该桌进行中账单。
3. shop/main 旧版兼容视图改为权威开桌成功后在后台更新，不再阻塞开始按钮。
4. 移除无法取消底层 Firestore 事务的30秒外层超时，避免页面先报失败、服务器稍后又成功。
5. 兼容视图同步失败时不再撤销已经成功的计时，并会明确显示“其他页面正在同步”。
6. 保留跨设备占桌保护：如果另一台设备已经开始该桌，不会重复开始或重复产生账单。

版本：v4.0.52
