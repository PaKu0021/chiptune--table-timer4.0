v4.0.41 Firestore 读取与弱网稳定性优化

1. 桌面二维码页面改为实时监听为主，服务器强制核对由每2秒降为每60秒。
2. 计时器页面服务器安全核对由每10秒降为每60秒。
3. 恢复联网或页面回到前台时仍会立即核对服务器状态。
4. 同一页面同一时刻只允许一个服务器核对请求，避免弱网请求堆积和连续覆盖界面。
5. 二维码页面单日主动读取量最多降低约96.7%，计时器页面主动读取量降低约83.3%。
6. 资源版本升级到 v4.0.41，避免浏览器继续使用旧版高频刷新脚本。
