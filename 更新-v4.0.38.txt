v4.0.38 系统反应速度优化

- 预约页不再每 3 秒无条件重建整张 14 桌时间表。
- 本机广播、浏览器存储和 Firestore 收到相同状态时只渲染一次。
- 预约页保留 10 秒一次的安全兜底检查，但只有数据实际变化才重绘。
- 计时器页跳过内容完全相同的云端/缓存快照，减少重复 DOM 重建。
- 正常操作仍立即上传；弱网队列的兜底扫描由 3 秒调整为 10 秒，避免多个已打开页面持续争抢 IndexedDB。
- 所有页面统一更新资源版本，避免浏览器继续使用旧的同步脚本缓存。
