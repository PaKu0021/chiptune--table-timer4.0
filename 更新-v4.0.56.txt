Chiptune 桌位管理系统 v4.0.56

iPad 待上传队列修复：

1. iPad 在云端成功响应前退到后台时，允许安全重放同一修改。
2. 云端字段已经等于本机目标值时，自动认定操作已完成，不再误判为多端冲突。
3. 已成功同步但仍显示等待上传的桌位、预约、编组和账单操作会在后台重试时自动清除。
4. 真正被其他设备改成不同内容的字段仍保留冲突保护，不会盲目覆盖。

版本：v4.0.56
