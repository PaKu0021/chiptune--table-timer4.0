Chiptune v4.0.34

1. 计时器桌位展开后不再重复显示桌号，桌号只保留在卡片顶部摘要。
2. 待上传操作的弱网重试间隔由 5 秒缩短到 3 秒。
3. 预约页面的本机状态刷新由 5 秒缩短到 3 秒。
4. 历史账单读取中断后的重试由 5 秒缩短到 3 秒。
5. 计时器主动向服务器核对共享状态保持为每 3 秒一次。

说明：操作保存后仍会立即启动后台上传；3 秒是断线重试与主动核对的
最长轮询间隔，实际到达云端的时间还会受到设备网络和 Firestore 响应影响。
